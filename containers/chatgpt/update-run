#!/usr/bin/with-contenv bash
set -u
next=0
while true; do
  now=$(date +%s)
  if (( now >= next )); then
    if update-chatgpt check; then next=$((now + 14400)); else next=$((now + 300)); fi
  else
    update-chatgpt apply || true
  fi
  sleep 5
done
